# 22. 括号生成 - 学习指南

> 🎯 **学习目标**：掌握回溯算法的经典应用，理解剪枝和决策树的概念

---

## 📚 前置知识

在开始之前，确保你了解：
- [x] 递归的基本概念
- [x] 字符串的基本操作
- [x] 回溯的基本思想
- [x] 什么是有效的括号

---

## 🎓 渐进式提示系统

### 💡 Level 1：问题理解

<details>
<summary>点击查看第一层提示</summary>

#### 核心问题
什么是"有效的"括号组合？

**有效括号的规则**：
1. 每个左括号 `'('` 必须有对应的右括号 `')'`
2. 右括号必须在对应的左括号之后出现
3. 在任意位置，右括号的数量不能超过左括号

#### 简化问题
先从小规模开始理解：

**n = 1**：
```
只有一种：()
```

**n = 2**：
```
有两种：(()) 和 ()()
```

**n = 3**：
```
有五种：
1. ((()))  - 全嵌套
2. (()())  - 部分嵌套
3. (())()  - 前嵌套
4. ()(())  - 后嵌套
5. ()()()  - 全并列
```

#### 无效的例子
```
)(      - 右括号在前
(()     - 左括号多了
())     - 右括号多了
```

#### 关键观察
在构造过程中的**任意时刻**：
- 已使用的右括号数量 ≤ 已使用的左括号数量
- 已使用的左括号数量 ≤ n

</details>

---

### 💡 Level 2：思路启发

<details>
<summary>点击查看第二层提示</summary>

#### 算法思路

**核心想法**：用回溯算法，逐个添加括号。

**决策**：在每一步，我们有两个选择：
1. 添加左括号 `'('`
2. 添加右括号 `')'`

**但不是所有选择都有效**！

#### 剪枝条件（关键！）

**选择1：添加左括号**
- 条件：已使用的左括号数量 `< n`
- 原因：总共只有 n 个左括号

**选择2：添加右括号**
- 条件：已使用的右括号数量 `<` 已使用的左括号数量
- 原因：否则会导致无效（右括号过多）

#### 决策树可视化（n=2）

```
                    ""
                 /      \
            "("          [剪枝：不能先加右括号]
           /    \
       "(("      "()"
       /         /  \
    "(()"    "()("  "())"
      |        |      [剪枝：右>左]
   "(())"   "()()"

有效的：["(())", "()()"]
```

#### 手动模拟

输入：n = 2

```
初始：current = "", left = 0, right = 0

步骤1：添加'('
  current = "(", left = 1, right = 0

步骤2：可以添加'('或')'
  分支1：添加'('
    current = "((", left = 2, right = 0
    
    步骤3：只能添加')'（left已满）
      current = "(()", left = 2, right = 1
      
      步骤4：添加')'
        current = "(())", left = 2, right = 2
        ✅ 收集结果："(())"
        回溯到步骤3
      
    回溯到步骤2
  
  分支2：添加')'
    current = "()", left = 1, right = 1
    
    步骤3：只能添加'('（right已满）
      current = "()(", left = 2, right = 1
      
      步骤4：添加')'
        current = "()()", left = 2, right = 2
        ✅ 收集结果："()()"
        回溯到步骤3
      
    回溯到步骤2

回溯到步骤1

结果：["(())", "()()"]
```

</details>

---

### 💡 Level 3：代码框架

<details>
<summary>点击查看第三层提示</summary>

#### 回溯框架

```cpp
class Solution {
public:
    vector<string> generateParenthesis(int n) {
        vector<string> result;
        string current = "";
        backtrack(result, current, 0, 0, n);
        return result;
    }
    
private:
    void backtrack(vector<string>& result, string& current, 
                   int left, int right, int n) {
        // TODO: 终止条件
        if (current.size() == 2 * n) {
            result.push_back(current);
            return;
        }
        
        // TODO: 选择1 - 添加左括号
        if (left < n) {
            current += '(';
            backtrack(result, current, left + 1, right, n);
            current.pop_back();  // 回溯
        }
        
        // TODO: 选择2 - 添加右括号
        if (right < left) {
            current += ')';
            backtrack(result, current, left, right + 1, n);
            current.pop_back();  // 回溯
        }
    }
};
```

#### 关键点

1. **参数说明**：
   - `result`：存储所有结果
   - `current`：当前正在构造的字符串
   - `left`：已使用的左括号数量
   - `right`：已使用的右括号数量
   - `n`：括号对数

2. **终止条件**：
   - `current.size() == 2 * n`（n对括号，共2n个字符）

3. **剪枝条件**：
   - 添加左括号：`left < n`
   - 添加右括号：`right < left`

4. **回溯操作**：
   - `current.pop_back()`：删除最后一个字符

</details>

---

### 💡 Level 4：完整实现

<details>
<summary>点击查看完整代码</summary>

#### 方法1：引用传递（推荐）

```cpp
class Solution {
public:
    vector<string> generateParenthesis(int n) {
        vector<string> result;
        string current = "";
        backtrack(result, current, 0, 0, n);
        return result;
    }
    
private:
    void backtrack(vector<string>& result, string& current, 
                   int left, int right, int n) {
        // 终止条件
        if (current.size() == 2 * n) {
            result.push_back(current);
            return;
        }
        
        // 添加左括号
        if (left < n) {
            current += '(';
            backtrack(result, current, left + 1, right, n);
            current.pop_back();  // 回溯
        }
        
        // 添加右括号
        if (right < left) {
            current += ')';
            backtrack(result, current, left, right + 1, n);
            current.pop_back();  // 回溯
        }
    }
};
```

#### 方法2：值传递（更简洁）

```cpp
class Solution {
public:
    vector<string> generateParenthesis(int n) {
        vector<string> result;
        backtrack(result, "", 0, 0, n);
        return result;
    }
    
private:
    void backtrack(vector<string>& result, string current, 
                   int left, int right, int n) {
        if (current.size() == 2 * n) {
            result.push_back(current);
            return;
        }
        
        if (left < n) {
            backtrack(result, current + "(", left + 1, right, n);
        }
        
        if (right < left) {
            backtrack(result, current + ")", left, right + 1, n);
        }
    }
};
```

**两种方法的区别**：
- **引用传递**：需要手动回溯（`pop_back()`）
- **值传递**：字符串自动拷贝，不需要手动回溯

</details>

---

## 🎯 动手实践

### 任务1：基础实现
实现回溯版本，测试 n=1, 2, 3。

**测试用例**：
```cpp
Solution sol;

// n = 1
auto result1 = sol.generateParenthesis(1);
// 输出：["()"]

// n = 2
auto result2 = sol.generateParenthesis(2);
// 输出：["(())", "()()"]

// n = 3
auto result3 = sol.generateParenthesis(3);
// 输出：["((()))", "(()())", "(())()", "()(())", "()()()"]
```

### 任务2：可视化调试
在纸上画出 n=2 的完整决策树。

### 任务3：添加打印
在代码中添加打印语句，观察回溯过程。

```cpp
void backtrack(vector<string>& result, string& current, 
               int left, int right, int n) {
    cout << "current: " << current 
         << ", left: " << left 
         << ", right: " << right << endl;
    
    // ... 其余代码
}
```

### 任务4：理解剪枝
修改剪枝条件，观察会生成哪些无效括号。

```cpp
// 去掉剪枝：right < left
if (right < n) {  // 改为 right < n
    current += ')';
    backtrack(result, current, left, right + 1, n);
    current.pop_back();
}
// 会生成什么？
```

---

## 🔍 深入理解

### 回溯三要素

1. **选择**：添加 `'('` 或 `')'`
2. **约束**：剪枝条件（`left < n` 和 `right < left`）
3. **目标**：生成完整的括号组合

### 回溯模板

```cpp
void backtrack(参数) {
    if (终止条件) {
        收集结果;
        return;
    }
    
    for (选择 in 选择列表) {
        if (剪枝条件) {
            continue;
        }
        做选择;
        backtrack(递归参数);
        撤销选择;  // 回溯
    }
}
```

### 为什么需要回溯？

**引用传递版本**：
```cpp
current += '(';           // 做选择
backtrack(...);           // 递归
current.pop_back();       // 撤销选择（回溯）
```

**原因**：`current` 是引用，递归返回后需要恢复原状，才能尝试其他选择。

**值传递版本**：
```cpp
backtrack(..., current + "(", ...);  // 直接传递新字符串
```

**原因**：`current + "("` 创建了新字符串，不影响原来的 `current`，自动回溯。

### 决策树的遍历顺序

**DFS（深度优先）**：
```
"" → "(" → "((" → "(()" → "(())" → 回溯 → "()" → ...
```

特点：一条路走到底，再回头。

---

## 📊 复杂度分析

### 时间复杂度
- **O(4^n / √n)**，即第 n 个卡塔兰数
- 有效括号序列数量为卡塔兰数：
  ```
  C_n = (2n)! / ((n+1)! * n!)
  ```
- 近似为 4^n / (n√n)

### 空间复杂度
- **O(n)**：递归栈深度最多为 2n

### 卡塔兰数
```
n    |  1  |  2  |  3  |  4   |  5   |  6    |
-----|-----|-----|-----|------|------|-------|
C_n  |  1  |  2  |  5  |  14  |  42  |  132  |
```

---

## 🚀 进阶挑战

### 挑战1：统计数量
不生成所有组合，只统计数量（卡塔兰数）。

**提示**：动态规划或公式计算。

### 挑战2：第k个括号组合
生成第 k 个（字典序）有效括号组合。

**提示**：使用卡塔兰数计算每个分支的数量。

### 挑战3：括号平衡检查
实现一个函数，检查给定字符串是否是有效括号。

```cpp
bool isValid(string s) {
    // TODO
}
```

### 挑战4：多种括号
扩展到三种括号：`()`, `[]`, `{}`。

---

## 🐛 常见错误

### 错误1：忘记回溯
```cpp
// ❌ 错误
void backtrack(vector<string>& result, string& current, 
               int left, int right, int n) {
    if (left < n) {
        current += '(';
        backtrack(result, current, left + 1, right, n);
        // 忘记 current.pop_back();
    }
}

// 结果：current会越来越长，错误！
```

### 错误2：剪枝条件错误
```cpp
// ❌ 错误（会生成无效括号）
if (right < n) {
    current += ')';
    // ...
}

// ✅ 正确
if (right < left) {
    current += ')';
    // ...
}
```

### 错误3：终止条件错误
```cpp
// ❌ 错误（可能提前终止）
if (left == n && right == n) {
    result.push_back(current);
    return;
}

// ✅ 正确
if (current.size() == 2 * n) {
    result.push_back(current);
    return;
}
```

### 错误4：引用 vs 值传递混淆
```cpp
// 引用传递（需要手动回溯）
void backtrack(string& current) {
    current += '(';
    backtrack(current);
    current.pop_back();  // 必须！
}

// 值传递（自动回溯）
void backtrack(string current) {
    backtrack(current + "(");  // 不需要pop_back
}
```

---

## 📝 自检清单

完成本题后，确保你能够：
- [ ] 理解回溯算法的基本流程
- [ ] 能够画出决策树
- [ ] 理解剪枝条件的原理
- [ ] 区分引用传递和值传递的区别
- [ ] 知道何时需要手动回溯
- [ ] 能够分析时间复杂度（卡塔兰数）
- [ ] 能够举一反三（其他回溯问题）

---

## 🎓 知识拓展

### 相关题目

1. **有效的括号**（LeetCode 20）
   - 用栈检查括号是否有效

2. **最长有效括号**（LeetCode 32）
   - 动态规划或栈

3. **删除无效的括号**（LeetCode 301）
   - BFS + 剪枝

4. **括号的最大嵌套深度**（LeetCode 1614）
   - 遍历统计

5. **不同的二叉搜索树II**（LeetCode 95）
   - 类似的回溯生成所有可能

### 卡塔兰数的应用

卡塔兰数出现在很多组合问题中：
1. **括号组合**：n对括号的有效组合数
2. **二叉树**：n个节点的不同二叉搜索树数量
3. **路径问题**：网格中不穿过对角线的路径数
4. **多边形三角剖分**：凸多边形的三角剖分数

### C++字符串技巧

```cpp
// 1. 拼接字符
string s = "hello";
s += '(';        // s = "hello("
s += ')';        // s = "hello()"

// 2. 删除最后一个字符
s.pop_back();    // s = "hello("

// 3. 字符串拷贝
string copy = s;  // 深拷贝

// 4. 字符串比较
if (s == "hello(") { }

// 5. 获取长度
int len = s.size();  // 或 s.length()

// 6. 清空
s.clear();  // s = ""

// 7. 字符串拼接（创建新字符串）
string new_s = s + "(";
```

---

## 💬 总结

**括号生成**是回溯算法的经典题目，重点掌握：

1. **回溯模板**：
   - 做选择 → 递归 → 撤销选择

2. **剪枝条件**（核心）：
   - 左括号：`left < n`
   - 右括号：`right < left`

3. **终止条件**：
   - `current.size() == 2 * n`

4. **两种实现方式**：
   - 引用传递：需要手动回溯
   - 值传递：自动回溯

5. **决策树思维**：
   - 画出决策树理解递归过程

**面试建议**：
- 优先使用回溯法（简洁清晰）
- 清晰解释剪枝条件的原因
- 能够画出决策树
- 知道时间复杂度与卡塔兰数相关

这道题是回溯的标准范例，理解了它就理解了回溯的精髓！🎯
