# 283. 移动零

## 📝 题目描述

给定一个数组 `nums`，编写一个函数将所有 `0` 移动到数组的末尾，同时保持非零元素的相对顺序。

**注意**：必须在原数组上操作，不能使用额外数组。

**示例 1：**
```
输入：nums = [0,1,0,3,12]
输出：[1,3,12,0,0]
```

**示例 2：**
```
输入：nums = [0]
输出：[0]
```

**约束条件：**
- `1 <= nums.length <= 10^4`
- `-2^31 <= nums[i] <= 2^31 - 1`

---

## 💡 解法一：辅助数组（不推荐）

### 思路
创建一个新数组，先放非零元素，再填充零。

### 代码实现
```cpp
void moveZeroes(vector<int>& nums) {
    vector<int> temp;
    int zeroCount = 0;
    
    // 收集非零元素
    for (int num : nums) {
        if (num != 0) {
            temp.push_back(num);
        } else {
            zeroCount++;
        }
    }
    
    // 补充零
    for (int i = 0; i < zeroCount; i++) {
        temp.push_back(0);
    }
    
    nums = temp;
}
```

### 复杂度分析
- **时间复杂度**：O(n)
- **空间复杂度**：O(n)

**缺点**：使用了额外空间，不符合题目要求。

---

## ⭐ 解法二：双指针（最优解）

### 思路

使用两个指针：
- **慢指针 `slow`**：指向下一个非零元素应该放置的位置
- **快指针 `fast`**：遍历数组，寻找非零元素

**核心操作**：
1. `fast` 遇到非零元素时，将其放到 `slow` 位置
2. `slow` 向后移动
3. 最后将 `slow` 之后的位置全部填充 0

### 代码实现（方法1：两次遍历）
```cpp
class Solution {
public:
    void moveZeroes(vector<int>& nums) {
        int slow = 0;
        
        // 第一次遍历：移动非零元素到前面
        for (int fast = 0; fast < nums.size(); fast++) {
            if (nums[fast] != 0) {
                nums[slow] = nums[fast];
                slow++;
            }
        }
        
        // 第二次遍历：后面填充零
        for (int i = slow; i < nums.size(); i++) {
            nums[i] = 0;
        }
    }
};
```

### 代码实现（方法2：一次遍历 + 交换）
```cpp
class Solution {
public:
    void moveZeroes(vector<int>& nums) {
        int slow = 0;
        
        for (int fast = 0; fast < nums.size(); fast++) {
            if (nums[fast] != 0) {
                // 只有当 slow != fast 时才交换，避免自己和自己交换
                if (slow != fast) {
                    swap(nums[slow], nums[fast]);
                }
                slow++;
            }
        }
    }
};
```

### 复杂度分析
- **时间复杂度**：O(n)
- **空间复杂度**：O(1)

---

## 🎯 核心知识点

### 1. 双指针技巧

**快慢指针**是处理数组原地操作的经典技巧：
- **慢指针**：指向结果数组的写入位置
- **快指针**：遍历原数组，筛选符合条件的元素

**适用场景**：
- 删除元素
- 移动元素
- 去重

### 2. 两种实现方式对比

| 方式 | 优点 | 缺点 |
|------|------|------|
| 两次遍历 | 逻辑清晰，易理解 | 需要两次循环 |
| 一次遍历 + 交换 | 效率更高 | 需要判断 `slow != fast` |

### 3. 为什么需要 `if (slow != fast)` 判断？

```cpp
nums = [1, 2, 3]  // 没有零的情况

不加判断：
fast=0, slow=0: swap(nums[0], nums[0])  // 自己和自己交换，浪费
fast=1, slow=1: swap(nums[1], nums[1])
...

加判断：
fast=0, slow=0: slow != fast? No, 跳过
fast=1, slow=1: slow != fast? No, 跳过
...
```

虽然结果正确，但避免无意义的交换操作可以提高效率。

---

## 🔍 图解过程

以 `nums = [0, 1, 0, 3, 12]` 为例（方法2：一次遍历 + 交换）：

```
初始状态：
nums = [0, 1, 0, 3, 12]
        ↑
      slow, fast

Step 1: fast=0, nums[0]=0 (是零，跳过)
nums = [0, 1, 0, 3, 12]
        ↑  ↑
      slow fast

Step 2: fast=1, nums[1]=1 (非零，交换)
nums = [1, 0, 0, 3, 12]
           ↑  ↑
         slow fast

Step 3: fast=2, nums[2]=0 (是零，跳过)
nums = [1, 0, 0, 3, 12]
           ↑     ↑
         slow   fast

Step 4: fast=3, nums[3]=3 (非零，交换)
nums = [1, 3, 0, 0, 12]
              ↑     ↑
            slow   fast

Step 5: fast=4, nums[4]=12 (非零，交换)
nums = [1, 3, 12, 0, 0]
                 ↑     ↑
               slow   fast

结束！
```

---

## 🎓 相似题目

### 1. 删除数组中的元素（LeetCode 27）
给定一个数组和一个值 `val`，原地删除所有等于 `val` 的元素。

**代码**：
```cpp
int removeElement(vector<int>& nums, int val) {
    int slow = 0;
    for (int fast = 0; fast < nums.size(); fast++) {
        if (nums[fast] != val) {
            nums[slow] = nums[fast];
            slow++;
        }
    }
    return slow;
}
```

### 2. 删除有序数组中的重复项（LeetCode 26）
原地删除重复元素，使每个元素只出现一次。

**代码**：
```cpp
int removeDuplicates(vector<int>& nums) {
    if (nums.empty()) return 0;
    int slow = 1;
    for (int fast = 1; fast < nums.size(); fast++) {
        if (nums[fast] != nums[slow-1]) {
            nums[slow] = nums[fast];
            slow++;
        }
    }
    return slow;
}
```

---

## 📌 记忆要点

- **模式**：原地修改数组 → 双指针（快慢指针）
- **慢指针**：指向写入位置
- **快指针**：遍历寻找符合条件的元素
- **优化**：用交换代替赋值 + 填充
- **复杂度**：O(n) 时间，O(1) 空间

---

## 🔨 实战技巧

### 技巧1：快慢指针模板
```cpp
int slow = 0;
for (int fast = 0; fast < nums.size(); fast++) {
    if (满足条件(nums[fast])) {
        nums[slow] = nums[fast];  // 或 swap
        slow++;
    }
}
// 后续处理（如填充、返回长度等）
```

### 技巧2：判断是否需要交换
- 如果只是筛选元素：直接赋值 `nums[slow] = nums[fast]`
- 如果需要保持原有元素：用交换 `swap(nums[slow], nums[fast])`

---

**标签**：`数组` `双指针` `easy`
